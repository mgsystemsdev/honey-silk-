[build]
builder = "RAILPACK"
buildCommand = "npm run build"

[deploy]
# Served via RAILPACK_SPA_OUTPUT_DIR=dist (Caddy). Fallback start if needed:
# startCommand = "npx --yes serve -s dist -l tcp://0.0.0.0:$PORT"
